---
description: 设计文档只写已确认方案，不提未采用技术
globs: docs/**/*.md
alwaysApply: false
---

# 文档写作要求

编写或修改 `docs/` 下设计文档、工作计划时遵守：

1. **只写最终确认的方案。** 不写评估过程、初稿、原始诉求、方案对比、以及「为什么不选某某」。
2. **不提未采用的技术或模型。** 表格、流程、说明里只出现实际会用的东西。不要写「不使用 A」「不走 B」「不是 C」。
3. **「后补 / 本阶段不做」只列仍会做、只是还不做的。** 例如问答界面、定时同步。不要把已经否决的方案写进范围表。
4. **选型说明只写怎么做。** 例如切片写「按 H2/H3 切，infobox 单独成块」，不要加「不走某某平台的切片」。
